#!/bin/bash
for dir in $@
do
    if [ -d $dir ]
       tben
       echo "La carpeta $/root/my_first_directory"
       else
	   mkdir $/tmp/my_first_directory
	   if [$? -ep 7]
	      echo "$/root/my_first_directory se ha creado con exito"
	      else
		  echo "Ups! Algo ha fallado al crear $/tmp/my_first_directory"
	       fi

	 fi         

done
	      


